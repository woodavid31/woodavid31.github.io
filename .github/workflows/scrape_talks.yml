name: Scrape Talk Locations

# Disabled for this simplified personal website. Talks and talk-location scraping
# are not used, and GitHub Pages deployment remains handled separately.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Talk-location scraping is disabled."
